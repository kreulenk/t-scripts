{
  "$id": "root",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "device": {
      "type": "string",
      "default": "someDevice",
      "examples": [
        "someDevice"
      ]
    },
    "commands": {
      "type": "array",
      "items": {
        "type": "string",
        "default": "show version",
        "examples": [
          "show version"
        ]
      }
    },
    "priority": {
      "type": "integer",
      "default": 4,
      "examples": [
        4
      ]
    }
  },
  "required": [
    "device",
    "commands",
    "priority"
  ],
  "additionalProperties": false
}